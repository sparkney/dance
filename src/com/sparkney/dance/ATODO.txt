ANOTHER TEST

180927
Dance should have a built in controller and a reserved context, like /com/sparkney/dance/*,
where we can serve resources like java JavaScripts, examples, etc.

180926
To implement scope, we are using long as key for IDs that is often generated by
System.nanoTime(). That generates an unnecessary amount of characters. 
An alternative is to use base 36 instead of base 10. Conversion is very fast.
1 million conversions in about 150 millis:

String key = Long.toString(System.nanoTime(), Character.MAX_RADIX);  //MAX_RADIX = 36

FAIL: An alternative method is to let the context maintain a counter initialized
by 0, and get the keys from the counter, then we would be able to get the shortest
possible unique keys. -> This fails, because if you load a component via ajax or
websocket, it will have a different context with IDs stating at 0 again, and the
they will clearly clash.



161010
Components should never take Object as parameter. Eg when Button takes an Object
in setOnClick(Object) and you set it to a Compnent, say Page, by misstake, there
will be an exeption in Renderer.onXxxx(Renderer.java:88) saying The instance of
class se.terminsstart.v1.adm.pages.Page is not supported. It is very hard to
locate the rumtime error. Is there even a reason why Button.setOnClick(..) scould
take an Object? The parameters are always one of the types String (for java script),
Action, AjaxAction or Location.


Add a component HyperText, that it just a text link with a hover feature. It's too
cumbersome to use Text and LinkPanel every time we need a simple text link. Maybe
we should change name of LinkPanel to HyperPanel? Or add setOnXxxx on every component?

Byt ut <b></b> mot <strong></strong>

HorizontalLayout måste använda display:inline-table/display:table-cell, går annars inte att centrera med text-align

VerticalLayout måste använda display:inline-block/display:block, går annars inte att centrera med text-align

Konstruktorn för Button ska ta knappnamnet som parameter, a la swing.

Döp om setText(..) till setLabel(..) för Button. Ska inte finnas setText(String) bara setText(Text)

Tag bort ny rad överallt för att optimera koden, eller gör det åtminstone valbart.

The center problem is that you can not user text-align to center en element with display:block,
only display:inline can be centered. Thus is why all outer elements have to be inline, inline-table,
inline-block, etc.

